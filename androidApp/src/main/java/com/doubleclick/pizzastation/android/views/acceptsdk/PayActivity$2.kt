package com.doubleclick.pizzastation.android.views.acceptsdk

import android.util.Log
import com.android.volley.Response
import com.android.volley.VolleyError


/*internal class `PayActivity$2`(`this$0`: PayActivity) :
    Response.ErrorListener {
    init {
        `this$0` = `this$0`
    }

    override fun onErrorResponse(error: VolleyError) {
        Log.d("notice", "json error output: $error")
        val networkResponse = error.networkResponse
        PayActivity.`access$000`(this.`this$0`)
        if (networkResponse != null && networkResponse.statusCode == 401) {
            PayActivity.`access$200`(this.`this$0`, "Invalid or Expired Payment Key")
        }
    }
}*/
